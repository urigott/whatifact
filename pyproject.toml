[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "confetti"
version = "0.1.0"
description = "A counter factual exploration tool"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=2.2.3",
    "pandas>=2.2.3",
    "shiny>=1.2.1",
]

[tool.setuptools]
packages = ["confetti"]

[tool.setuptools.package-data]
confetti = ["css/*.css"]
